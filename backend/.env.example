# NBA Performance Predictor - Backend Environment Variables

# Frontend URL for CORS (required for production)
# Example for Vercel: https://nba-performance-predictor.vercel.app
# Leave empty for local development (uses localhost origins only)
FRONTEND_URL=

# Optional: Gemini API Key for AI-powered insights
# If not provided, AI insights will use fallback responses
GEMINI_API_KEY=

# Optional: Custom Gemini API URL
# Only set if using a custom endpoint
GEMINI_API_URL=

# Optional: Custom CA bundle for SSL verification
# Required if behind corporate proxy or custom SSL certificates
GEMINI_CA_BUNDLE=
REQUESTS_CA_BUNDLE=
